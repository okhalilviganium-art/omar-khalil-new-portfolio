import RecycleBinContent from "@/components/dashboard/recycle-bin/RecycleBinContent";

export const dynamic = "force-dynamic";
export const metadata = { title: "Recycle Bin — Dashboard" };

export default function RecycleBinPage() {
  return <RecycleBinContent />;
}
